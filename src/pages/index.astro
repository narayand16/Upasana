---
import { getCollection } from 'astro:content';
import type { BookEntry } from '@/types';
import Library from '@/lib/library.astro';

// Fetch all books
const books: BookEntry[] = await getCollection('books');
---

<Library books={books} />
